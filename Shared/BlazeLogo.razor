<span class="text-amber-400 tracking-tighter text-center whitespace-nowrap">
    @if (!Short)
    {
        <span class="oi" data-glyph="fire">Blaze</span>
        <span class="text-white">Weather</span>
    } else {
        <span class="oi" data-glyph="fire"></span>
    }
</span>

@code {
    [Parameter] public bool Short { get; set; } = false;
} 
